name: Docs

on:
  pull_request:
    paths:
      - 'docs/**'
      - 'zensical.toml'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
      - name: Build docs
        run: uvx zensical build
